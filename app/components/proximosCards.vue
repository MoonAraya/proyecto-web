<script setup lang="es">
    // import type { Evento } from '~/types/Evento';

    // const props = defineProps<{
    //     evento: Evento
    // }>()

    // const fechaFormateada = computed(() => formatFechaCorta(props.evento.fecha))
    // const horaFormateada = computed(() => formatHora(props.evento.fecha))
</script>
<template>
    <!-- 
        <div
        class="bg-brand-surface border border-brand-border rounded-2xl p-5 hover:border-brand-purple transition-colors group">
        <div class="flex items-start justify-between mb-3">
            <span
                class="bg-brand-magenta/20 text-brand-magenta text-xs font-semibold px-2.5 py-1 rounded-full border border-brand-magenta/30">
                {{ evento.categoria.nombre }}
            </span>
        </div>
        <h3 class="text-brand-text font-bold text-lg mb-4 group-hover:text-brand-purple transition-colors">
            {{ evento.nombre }}
        </h3>
        <div class="flex flex-col gap-2 text-sm">
            <div class="flex items-center justify-between text-brand-muted">
                <span class="font-medium text-brand-text/70">Fecha</span>
                <span>{{ fechaFormateada }}</span>
            </div>
            <div class="flex items-center justify-between text-brand-muted">
                <span class="font-medium text-brand-text/70">Hora</span>
                <span>{{ horaFormateada }}</span>
            </div>
            <div class="flex items-center justify-between border-t border-brand-border pt-2 mt-1">
                <span class="font-medium text-brand-text/70 text-sm">Cupos</span>
                <span class="text-brand-yellow font-semibold">{{ evento.cupo }} disponibles</span>
            </div>
        </div>
    </div> -->
</template>